# bench-history-dock bundle layer: inserts the plugin row.
# (exam fixture, distilled from @huanlin/dsh-plugin-input-history; the IME
# guard and caret-geometry helpers are elided — the exam is the composer
# surface + the keydown listener placement + the history-collection slice)
- insert:
    - id: bench-history-dock
      name: '@bench/dsh-bench-history-dock'
      config:
        capacity: 500
