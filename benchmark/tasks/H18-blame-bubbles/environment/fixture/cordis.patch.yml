# bench-blame-bubbles bundle layer: inserts the plugin row.
# (exam fixture, distilled from @huanlin/dsh-plugin-auto-blame; the LLM
# generation body and the settings seam are elided — the exam is the RPC
# contract, the projection state map, and the client plane)
- insert:
    - id: bench-blame-bubbles
      name: '@bench/dsh-bench-blame-bubbles'
      config:
        enabled: true
