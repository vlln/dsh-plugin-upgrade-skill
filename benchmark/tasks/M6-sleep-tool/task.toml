schema_version = "1.4"
# Harbor task config: dsh plugin migration exam M6 (host-only tool plugin cohort contract, in-container headless cold boot)
[task]
name = "dsh-plugin-upgrade/m6-sleep-tool"
version = "1.1.0"
description = "Migrate a 0.1.1-era host-only tool plugin to the 0.1.2-alpha.2 cohort: bare cordis contract, dsh-llm ContentBlock move, peer floor rewrite, and a headless activation proof"
keywords = ["dsh", "plugin-migration", "cohort", "peer-range", "tool-plugin", "cold-boot", "container", "harbor"]

[metadata]
difficulty = "medium"
category = "programming"
tags = ["dsh", "plugin-upgrade", "cohort", "peer-dependencies", "english-prompt"]
execution_contract = "BENCHMARK-AUTH-v1"

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 600.0

[environment]
network_mode = "public"
cpus = 1
memory_mb = 2048
storage_mb = 4096
