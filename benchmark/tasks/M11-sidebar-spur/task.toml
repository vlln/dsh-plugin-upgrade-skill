schema_version = "1.4"
# Harbor task config: dsh plugin migration exam M11 (client-plane dock widget, in-container cold boot verification)
[task]
name = "dsh-plugin-upgrade/m11-sidebar-spur"
version = "1.1.0"
description = "Migrate a 0.1.1-era client-only dock widget to 0.1.2-alpha.2: client-runtime removal, the slots service's move to ui-renderer, client inject recomposition, and a browser-roster activation proof"
keywords = ["dsh", "plugin-migration", "client-plane", "ui-slots", "client-runtime", "cold-boot", "container", "harbor"]

[metadata]
difficulty = "medium"
category = "programming"
tags = ["dsh", "plugin-upgrade", "client-plane", "ui-slots", "english-prompt"]
execution_contract = "BENCHMARK-AUTH-v1"

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 600.0

[environment]
network_mode = "public"
cpus = 1
memory_mb = 2048
storage_mb = 4096
