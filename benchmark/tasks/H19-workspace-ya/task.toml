schema_version = "1.4"
# Harbor task config: dsh plugin migration exam H19 (web client composition takeover, in-container cold boot verification)
[task]
name = "dsh-plugin-upgrade/h19-workspace-ya"
version = "1.1.0"
description = "Migrate a 0.1.1-era workspace-browser replacement plugin to 0.1.2-alpha.2: client-runtime split, provideRoot + GlobalStandardProps.useWorkspaces takeover, uiWorkspace service stand-in, and a browser-roster activation proof"
keywords = ["dsh", "plugin-migration", "client-plane", "composition", "ui-workspace", "cold-boot", "container", "harbor"]

[metadata]
difficulty = "hard"
category = "programming"
tags = ["dsh", "plugin-upgrade", "client-composition", "slots-provideRoot", "english-prompt"]
execution_contract = "BENCHMARK-AUTH-v1"

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 600.0

[environment]
network_mode = "public"
cpus = 1
memory_mb = 2048
storage_mb = 4096
