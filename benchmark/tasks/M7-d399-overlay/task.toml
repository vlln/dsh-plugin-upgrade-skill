schema_version = "1.4"
# Harbor task config: dsh plugin migration exam M7 (client-only web plugin, client-runtime deletion + inject recomposition, in-container web cold boot + browser roster)
[task]
name = "dsh-plugin-upgrade/m7-d399-overlay"
version = "1.1.0"
description = "Migrate a 0.1.1-era client-only game overlay plugin to 0.1.2-alpha.2: deleted dsh-client-runtime, client inject recomposition (locale + api-session-controller), ISessions type-source repoint, and a browser-roster activation proof"
keywords = ["dsh", "plugin-migration", "client-plane", "inject", "peer-cohort", "type-surface", "cold-boot", "container", "harbor"]

[metadata]
difficulty = "medium"
category = "programming"
tags = ["dsh", "plugin-upgrade", "client-runtime", "inject", "peer-cohort", "english-prompt"]
execution_contract = "BENCHMARK-AUTH-v1"

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 600.0

[environment]
network_mode = "public"
cpus = 1
memory_mb = 2048
storage_mb = 4096
