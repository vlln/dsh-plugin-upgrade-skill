schema_version = "1.4"
# Harbor task config: dsh plugin migration exam M10 (client-runtime split migration, web two-plane, in-container cold boot verification)
[task]
name = "dsh-plugin-upgrade/m10-tools-tree"
version = "1.1.0"
description = "Migrate a 0.1.0-era tools-management panel plugin to 0.1.2-alpha.2: the ctx.slots service moved to dsh-client-ui-renderer in the client-runtime split — rewire the type-only Context merges, drop the deleted runtime from the client inject, and prove the settings.section tab on the browser roster"
keywords = ["dsh", "plugin-migration", "client-runtime-split", "client-plane", "slots", "settings-panel", "cold-boot", "container", "harbor"]

[metadata]
difficulty = "medium"
category = "programming"
tags = ["dsh", "plugin-upgrade", "client-runtime-split", "client-plane", "english-prompt"]
execution_contract = "BENCHMARK-AUTH-v1"

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 600.0

[environment]
network_mode = "public"
cpus = 1
memory_mb = 2048
storage_mb = 4096
