# bench-tools-tree bundle layer: inserts the plugin row.
# (exam fixture, distilled from @huanlin/dsh-plugin-tools-manager; the tool
# registry + policy + gateway body is elided)
- insert:
    - id: bench-tools-tree
      name: '@bench/dsh-bench-tools-tree'
      config:
        disabled: []
