# bench-mcpanel bundle layer: inserts the plugin row.
# (exam fixture, distilled from @huanlin/dsh-plugin-mcp-manager; the yaml
# registry + /api routes + mcp_* tools body is elided)
- insert:
    - id: bench-mcpanel
      name: '@bench/dsh-bench-mcpanel'
