schema_version = "1.4"
# Harbor task config: dsh plugin migration exam M9 (client-runtime split migration, web two-plane, in-container cold boot verification)
[task]
name = "dsh-plugin-upgrade/m9-mcpanel"
version = "1.1.0"
description = "Migrate a 0.1.2-era MCP-server management panel plugin to 0.1.2-alpha.2: dsh-client-runtime split (client inject recomposition, locale namespace declaration on the ui-slots LocaleNamespaceMap), scoped-cordis peer rewrite, and a browser-roster activation proof"
keywords = ["dsh", "plugin-migration", "client-runtime-split", "client-plane", "settings-panel", "locale-namespace", "cold-boot", "container", "harbor"]

[metadata]
difficulty = "medium"
category = "programming"
tags = ["dsh", "plugin-upgrade", "client-runtime-split", "client-plane", "english-prompt"]
execution_contract = "BENCHMARK-AUTH-v1"

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 600.0

[environment]
network_mode = "public"
cpus = 1
memory_mb = 2048
storage_mb = 4096
