# bench-mineru-api bundle layer: inserts the plugin row.
# (exam fixture, distilled from @huanlin/dsh-plugin-mineru; tool body elided)
- insert:
    - id: bench-mineru-api
      name: '@bench/dsh-bench-mineru-api'
      config:
        baseURL: https://mineru.net
        defaultBackend: pipeline
